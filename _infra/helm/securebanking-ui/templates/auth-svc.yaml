apiVersion: {{ .Values.apiVersion.service }}
kind: Service
metadata:
  name: {{ .Chart.Name }}-auth
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: {{ .Values.auth.deployment.port }}
    protocol: TCP
  selector:
    app: {{ .Chart.Name }}-auth