apiVersion: {{ .Values.apiVersion.service }}
kind: Service
metadata:
  name: {{ .Chart.Name }}-{{ .Values.swagger.subdomain }}
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: {{ .Values.auth.deployment.port }}
    protocol: TCP
  selector:
    app: {{ .Chart.Name }}-{{ .Values.swagger.subdomain }}