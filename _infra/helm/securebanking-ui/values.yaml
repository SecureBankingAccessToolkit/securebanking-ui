deployment:
  apiVersion: apps/v1
  image:
    # repo: 
    # tag:
    pullPolicy: Always
  port: 80
  replicas: 1
  resources: {}
  rollingUpdate:
    maxSurge: 50%
    maxUnavailable: 25%
  strategyType: RollingUpdate
  templateName: forgerock

service:
  port: 80
  apiVersion: v1
