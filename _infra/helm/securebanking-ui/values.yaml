deployment:
  apiVersion: apps/v1
  image:
    # repo: 
    # tag:
    pullPolicy: Always
  port: 80
  replicas: 1
  resources: {}
  rollingUpdate:
    maxSurge: 50%
    maxUnavailable: 25%
  strategyType: RollingUpdate
  templateName: forgerock

service:
  apiVersion: v1
  port: 8080
  targetPort: 80
